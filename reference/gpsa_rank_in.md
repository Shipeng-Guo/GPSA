# Get rank of a signature in a result data.frame

Get rank of a signature in a result data.frame

## Usage

``` r
gpsa_rank_in(df, signature_id)
```

## Arguments

- df:

  Result data.frame with signature_id column

- signature_id:

  Signature ID to find

## Value

Integer rank (row number) or NA
